---
import BaseLayout from '../../layouts/BaseLayout.astro'
import { getLangFromUrl, useTranslations } from '../../i18n/utils'
import { author, siteTitle } from '../../constants'

const lang = getLangFromUrl(Astro.url)
const t = useTranslations(lang)
const subtitle = t('nav.about') + ' de mí'
const pageTitle = siteTitle + ' - ' + subtitle
const identity = {
  name: author.name,
  country: 'España',
  hobbies: ['leer novelas de ciencia ficción', 'senderismo', 'natación', 'juegos de mesa'],
  occupation: 'desarrollador de software'
}
const skills = ['CSS', 'Git', 'HTML', 'JavaScript', 'Laravel', 'MySQL', 'PHP', 'React', 'Tailwind CSS', 'TypeScript', 'Vue']
---
<BaseLayout pageTitle={pageTitle}>
  <h1>{subtitle}</h1>
  <p>Soy {identity.name}. Vivo en {identity.country} y trabajo como {identity.occupation}.</p>
  <p>Soy un jugador de equipo al que le gusta trabajar en equipos ágiles. La cobertura de test es imprescindible. Me gusta llamar a las cosas por su nombre técnico cuando hablo de código, prefiriendo la comunicación asertiva. La calidad del código, el mantenimiento y la corrección de errores son responsabilidad de todo el equipo para mí.</p>
  <p>Algunas de mis aficiones son {new Intl.ListFormat('es-ES', { style: 'long', type: 'conjunction' }).format(identity.hobbies)}.</p>
  {!!skills.length &&
    <p>Mis habilidades técnicas incluyen:</p>
    <ul>{skills.map(skill => <li>{skill}</li>)}</ul>
  }
</BaseLayout>

<style is:global>
  main {
    align-self: center;
    max-width: 760px;
		width: calc(100% - 2rem);
  }
</style>